<%= bootstrap_form_with(model: status, local: true) do |form| %>
  <% if status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(status.errors.count, "error") %> prohibited this status from being saved:</h2>

      <ul>
        <% status.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :Статус %>
    <%=  form.text_field :funny, skip_label: true %>
  </div>
  <div class="field">
    <%= form.label :'Основной'%>
    <%=  form.text_field :general, skip_label: true %>
  </div>

  <div class="field">
    <%= form.label :'Над кнопкой'%>
    <%=  form.text_field :statum, skip_label: true %>
  </div>
  <div class="actions">
    <%= form.submit :Обновить %>
  </div>
<% end %>
