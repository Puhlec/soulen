<div class="form-group">
  <%= bootstrap_form_with model: [template.instructor, template], local: true do |form| %>
    <% if template.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(template.errors.count, "error") %> prohibited this student from being saved:</h2>

        <ul>
          <% template.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div> 
      <%= form.label :текст %>
      <%= form.text_area :pictogram, skip_label: true, class: 'form-control form-control-lg' %>
    </div>
    <div>
      <%= form.submit :"Создать запись шаблона", class: 'btn btn-info' %>
    </div>
  <% end %>
</div>

