<center>
  <%= link_to 'Общие', settings_path(@current_instructor), class: 'btn btn-dark' %>
  <%= link_to 'Шаблоны', templates_path(@current_instructor), class: 'btn btn-dark' %>
  <%= link_to 'Архивные', archives_path(@current_instructor), class: 'btn btn-dark' %>
</center>
