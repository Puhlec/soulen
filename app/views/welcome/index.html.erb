<div class="bg">
  <div>
    <h1> <%= @status.general %> </h1>
  </div>
  <div>
    <h2> <%= @status.funny %> </h2>
  </div>
  <div class="cent">  
    <%= link_to 'Войти', login_path, class: 'btn-lg btn-dark', remote: true %>
  </div>
  <div class ="center">
    <h1>  <%= @status.statum %> </h1>
    <% if @current_admin %>
      <%= link_to '', edit_status_path(@status), class: 'oi oi-pencil', remote: true %>
    <% end %>
  </div>
</div>
